#include <stdio.h>
#include <math.h>
#include <time.h>
#include <stdlib.h>
#include <string.h>

int main(int Cantidad_De_Argumentos, const char * argumento[]){
/*              Calculo de la hipotenusa
    int a, b;

    a= atoi(argumento[1]);
    b= atoi(argumento[2]);

    printf("La Hipotenusa mide %f\n", sqrt(pow(a,2)+pow(b,2)));
*/

/*              Generacion de numeros Aleatorios
    srand(time(NULL));
    printf("%d\n", rand());
*/

/*              Calculo de fechas despues de x dias
    time_t oneDay= 24*60*60, today=time(NULL), nextDate;
    nextDate= today +oneDay*atoi(argumento[1]);

    printf("%s\n", ctime(&nextDate));
*/

    return 0 ;
}